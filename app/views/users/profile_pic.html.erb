<%= form_for @user1,url: { :action => 'update' }, html: { multipart: true } do |form| %>
  <%= form.file_field :photo %>
   <%= form.submit "upload", id: "upload", class:"btn btn-success" %>
<% end %>